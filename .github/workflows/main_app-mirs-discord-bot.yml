# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Deploy ASP.NET Core app to Azure Web App

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
    AZURE_WEBAPP_NAME: app-mirs-discord-bot
    AZURE_WEBAPP_PACKAGE_PATH: '.'
    DOTNET_VERSION: '10.x'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Set up .NET Core
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }} 

      - name: dotnet build and publish Discord Bot 
        working-directory: ./src/MiRs.Discord.Bot.API
        run: |
          dotnet restore
          dotnet build --configuration Release
          dotnet publish -c Release -o '../../${{ env.AZURE_WEBAPP_PACKAGE_PATH }}/myapp' 
      
      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_B429929711634C11ADF37C848F54B888 }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_E3098235D2864A42BB31F997D605D56D }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_746AFAC0E3494355B0A1B1406D2681B7 }}

      - name: Deploy to Azure Web App
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v3
        with:
          app-name:  ${{ env.AZURE_WEBAPP_NAME }}
          package: '${{ env.AZURE_WEBAPP_PACKAGE_PATH }}/myapp'
          